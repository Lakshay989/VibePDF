// SPEC: P1-VIEW-008 — collapsible thumbnails sidebar with lazy generation.
// Stub for the bootstrap commit. Real implementation lands in a Phase 1
// follow-up that wires PDFium's render-to-bitmap through the document
// actor and caches thumbnails in IndexedDB.
export function ThumbnailPanel() {
  return (
    <aside className="hidden w-40 border-r border-neutral-200 p-2 text-xs text-neutral-500 dark:border-neutral-800">
      Thumbnails (Phase 1 follow-up)
    </aside>
  );
}
