// SPEC: P1-VIEW-009 — outline sidebar when the document has one.
// Stub; real tree comes from `PDFDocumentProxy.getOutline()` in a
// Phase 1 follow-up.
export function OutlinePanel() {
  return (
    <aside className="hidden w-56 border-r border-neutral-200 p-2 text-xs text-neutral-500 dark:border-neutral-800">
      Outline (Phase 1 follow-up)
    </aside>
  );
}
