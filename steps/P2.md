# Phase 2 — Page operations (decoupled steps)

> Goal (per `docs/05_ROADMAP.md`): move, copy, delete, merge, split,
> extract, rotate pages — and finally **save real PDFs as output**.
>
> Phase 2 is also where the project earns the right to mutate bytes.
> Every write path lands behind the document actor and round-trips
> through PDFium before returning success — see [[B1]] in `steps/P1.md`.

## Acceptance demo (from roadmap)

- Open `tests/fixtures/acceptance/p2-multi.pdf`, delete page 3, insert a blank, rotate page 5 by 90°, save. Re-open in Adobe Acrobat: changes are visible, file is not corrupt.
- Merge 5 PDFs from `tests/fixtures/acceptance/p2-merge-*.pdf`. Form fields and bookmarks survive.
- Split a 100-page PDF into 10 files. Each file opens cleanly.

## Spec coverage

`P2-PAGE-001` through `P2-PAGE-010` (10 spec lines). Plus 3 infrastructure steps (save, autosave, undo/redo) with no `P2-PAGE-*` ID.

---

## Track A — Persistence infrastructure (must land before any B/C/D step)

### P2.A1. `[x]` Save button
- **Status:** Done. Atomic temp+rename, `.bak` rotation, round-trip verify,
  dirty-flag no-op. `check` + `test` (70/70) + `test:rust` (incl. 3
  `save_noop` cases) green. Human verified the saved artifact opens cleanly in
  Preview + Acrobat + a third reader, and re-opens in VibePDF itself (clean
  GUI dev session, no panics; also exercised a real 4-page statement).
  Deviation from the doc below: the Cmd/Ctrl+S binding lives in a new
  `src/app/use-save.ts` hook (matching the `use-file-open.ts` pattern) rather
  than inline in `App.tsx`; save logic lives in `pdf/document.rs` (which
  `docs/04` already allocates to "Open, save, metadata") rather than a new
  `pdf/save.rs`.
- **Spec:** infrastructure
- **Touches (new):** `src-tauri/src/commands/save.rs`, `src/ipc/save.ts`
- **Touches (modify):** `src/app/App.tsx` (Cmd/Ctrl+S binding), `src-tauri/src/pdf/actor.rs` (`Message::Save { path }`)
- **What:** Write the current PDFium document to disk. If `path` matches the original, rotate the previous file as `<name>.bak` (kept for one save cycle only). Round-trip the saved bytes through PDFium before reporting success; reject on read-back failure.
- **Acceptance:** Open hello.pdf → save → file size and SHA-256 unchanged (no-op save is a no-op). Edit (via a later step) → save → re-open in PDFium externally, no parse errors.
- **Test:** `src-tauri/tests/save_noop.rs` — open, save, assert byte-identical (or at least PDFium-identical after canonicalization).
- **Depends on:** P1.B1 (real actor).

### P2.A2. `[~]` Auto-save + crash recovery
- **Status:** Machinery landed — `pdf/autosave.rs` (write/scan/discard +
  the 30s std-thread tick), actor `Autosave` message + dir/cleanup,
  `recovery_list`/`recovery_discard` commands, `use-recovery.ts` +
  `RecoveryDialog.tsx`. `check` + `test` (77/77) + `test:rust` (incl. 4
  `autosave` cases) green. `[~]` not `[x]` because the live loop is dormant
  until something makes a doc dirty: the autosave-only-when-dirty write and
  the "force-kill → relaunch → recover" acceptance demo land with **P2.B2
  (delete)**. Deviations from the doc: the tick is a dedicated **std thread**
  (not `tokio::time` — that feature isn't enabled); autosave logic lives in
  `pdf/autosave.rs`; recovery "Recover" reopens the autosave file (adopting
  the *original* path so ⌘S targets it is a noted BACKLOG refinement).
- **Spec:** infrastructure
- **Touches (new):** `src-tauri/src/pdf/autosave.rs`, `src-tauri/src/commands/recovery.rs`
- **Touches (modify):** `src-tauri/src/lib.rs` (spawn the 30s tick task)
- **What:** Every 30 s, if the document is dirty, write a copy to `<app_data_dir>/autosave/<documentId>.pdf`. On startup, scan that dir; for each entry, offer a recovery dialog ("Recover unsaved changes to foo.pdf?").
- **Acceptance:** Edit a doc, force-kill the app, relaunch → recovery dialog appears. Accept → unsaved edits restored.
- **Test:** `src-tauri/tests/autosave.rs` — simulate dirty doc, advance time, assert file written; simulate crash by reusing the autosave dir; assert recovery payload matches.
- **Depends on:** P2.A1.

### P2.A3. `[x]` Undo/redo stack
- **Human-verified (2026-06-13):** ⌘Z / ⌘⇧Z revert + re-apply both views after rotate/delete; toolbar buttons enable/disable correctly.
- **Status:** Machinery landed — generic `UndoStack<T>` + `Edit<T>` command
  pattern (`pdf/undo.rs`, 4 unit tests), actor `Undo`/`Redo`/`GetHistoryState`
  messages, `pdf_undo`/`pdf_redo`/`pdf_history_state` commands,
  `history-store.ts` + `use-history.ts` (Cmd/Ctrl+Z, Cmd/Ctrl+Shift+Z),
  Undo/Redo toolbar buttons. `check` + `test` (76/76) + `test:rust` green.
  `[~]` not `[x]` because the acceptance as written ("delete pages 3,5,7 →
  undo") needs a real page op: it lands end-to-end with **P2.B2 (delete)**,
  which adds the first `Edit<PdfDocument>`. Until then the stack is always
  empty and undo/redo are verified no-ops. Architecture documented in
  `docs/04` § "Undo/redo (session history)".
- **Spec:** infrastructure
- **Touches (new):** `src-tauri/src/pdf/undo.rs`, `src/state/history-store.ts`
- **Touches (modify):** `src-tauri/src/pdf/actor.rs` (every mutating message pushes an inverse onto the stack)
- **What:** Per-document undo stack with **page-level** granularity (move, insert, delete, rotate, crop, resize each as one undoable action). Cmd/Ctrl+Z / Cmd/Ctrl+Shift+Z.
- **Acceptance:** Delete pages 3, 5, 7 → undo three times → all pages restored in correct order. Redo three times → same deletions reapplied.
- **Test:** `src-tauri/tests/undo_redo.rs` — sequence of mutations on hello.pdf, undo to start, assert page tree identical.
- **Depends on:** P2.A1.

---

## Track B — Single-page operations

### P2.B1. `[x]` Rotate page(s)
- **Human-verified (2026-06-13):** rotated artifact renders rotated in Preview; in-app rotate (right/left/180) updates thumbnail + main view live.
- **Status:** Landed — the first real edit. `RotateEdit: Edit<PdfDocument>`
  (`pdf/rotate.rs`) sets PDFium `/Rotate`, integrates with undo (A3) + dirty
  (A1/A2); actor `RotatePages` message; `pdf_rotate_pages` command; thumbnail
  right-click `RotateMenu` with live thumbnail refresh (cache-invalidate +
  re-render) and Undo-button sync. `check` + `test` (81/81) + `test:rust`
  (rotate.rs: 4 — persist/undo/redo/atomic-on-error) green. `[~]` not `[x]`
  until the human confirms `/tmp/vibepdf-verify-rotated.pdf` (also on the
  Desktop) renders rotated in Acrobat + Preview + a third reader.
- **Surfaced + fixed a real concurrency bug:** PDFium isn't thread-safe across
  documents (page-ops/save/close raced → SIGABRT/SIGSEGV). Unified all PDFium
  FFI under one process-global `PDFIUM_LOCK` (was render-only), close the
  actor's doc under it, and run the Rust tests single-threaded. See `docs/04`.
- **Deferred (BACKLOG):** live **main-view** preview of edits (PDF.js updates
  on save/reopen only) + undo/redo-driven view refresh — the shared
  "edit-preview pipeline" for all of B2–B5.
- **Spec:** P2-PAGE-001
- **Touches (new):** `src-tauri/src/pdf/rotate.rs`, `src/tools/rotate/`
- **Touches (modify):** `src/panels/ThumbnailPanel.tsx` (context-menu entry)
- **What:** Rotate single page, range, or all pages by 90° / 180° / 270°. Persisted as PDFium `/Rotate` on the page dict, not a viewer transform.
- **Acceptance:** Rotate page 1 of hello.pdf 90° → save → open externally in Preview.app → page renders rotated. Reopen in VibePDF → rotation persisted.
- **Test:** `src-tauri/tests/rotate.rs` — rotate, save, re-open, assert `/Rotate` on page 0.
- **Depends on:** P2.A1, P2.A3.

### P2.B2. `[x]` Delete page(s)
- **Human-verified (2026-06-13):** deleted artifact + in-app delete/undo confirmed; dangling-ref prune confirmed (delete target page → save → reopen → broken bookmark/link gone; verified in `pruned.pdf` bytes: Ch2 removed).
- **Status:** Landed. `DeleteEdit: Edit<PdfDocument>` (`pdf/delete_page.rs`)
  deletes via `PdfPage::delete`; its inverse stashes the removed pages in a
  holding doc (bytes) and re-imports them → undo restores content **and**
  order. Actor `DeletePages`; `pdf_delete_pages`; thumbnail context-menu
  "Delete page" + Delete/Backspace key; inherits live preview + autosave.
  `check` + `test` (85/85) + `test:rust` (delete_page: 5 + the reference-
  integrity canary) green. `[~]` until the human checks
  `/tmp/vibepdf-verify-deleted.pdf` (also on Desktop) in Acrobat + Preview +
  a third reader, and confirms the GUI flow (delete/undo/live preview).
- **Reference integrity — both halves now done.** Surviving internal
  links/bookmarks/dests track renumbering automatically (PDF destinations are
  indirect object refs, not indices) — proven by a test on `links.pdf`. And
  references *to* a deleted page (which dangle) are now **pruned on save**:
  `cos::prune_dangling_destinations` (lopdf) runs in `save_document`, removing
  broken `/Link` annotations and bookmarks + GC'ing orphans. So a delete →
  save → reopen has no broken links/bookmarks. Same prune cleans split (C3) /
  extract (C2) outputs. Tests: `delete_prunes_dangling_link/bookmark`,
  `split_prunes_cross_file_link`, `cos_*`. Fully satisfies P2-PAGE-003 "update
  internal references." (Named-dest cleanup → BACKLOG.)
- **UI scope:** single-page delete (backend takes `Vec` for future multi-select).
- **Spec:** P2-PAGE-003
- **Touches (new):** `src-tauri/src/pdf/delete_page.rs`
- **Touches (modify):** `src/panels/ThumbnailPanel.tsx` (Delete key handler)
- **What:** Remove the page from the page tree, renumber, update link/bookmark/named-destination references that point to surviving pages.
- **Acceptance:** Delete page 3 of a 5-page doc with an internal link to page 4 → save → re-open → link now targets the new page 3.
- **Test:** `src-tauri/tests/delete_page.rs` — assert page count drops; assert internal links rewritten.
- **Depends on:** P2.A1, P2.A3.

### P2.B3. `[x]` Insert blank page
- **Human-verified (2026-06-13):** inserted artifact + in-app insert/undo confirmed. (The "bookmark disruption" seen in testing was an artifact of saving over the live committed fixture, not a code bug — bookmark dests are by-reference and survive insert.)
- **Status:** Landed. `InsertBlankEdit: Edit<PdfDocument>` (`pdf/insert_blank.rs`)
  via `create_page_at_index`, inheriting the adjacent page's dimensions
  (size + orientation) unless a size is given. **Inverse reuses `DeleteEdit`**
  (B2) — undo removes the blank, redo re-inserts. Actor `InsertBlankPage`;
  `pdf_insert_blank_page` (optional `width`/`height` override); thumbnail
  context-menu "Insert blank page after"; full-reload preview (page-tree
  change) + autosave + undo, all inherited. `check` + `test` (97/97) +
  `test:rust` (insert_blank: 4) green. `[~]` until the human checks
  `/tmp/vibepdf-verify-inserted.pdf` (Desktop) in Acrobat + Preview + a third
  reader, and the GUI flow.
- **UI scope:** insert-after via the menu (inherits size); the optional
  custom-size override is IPC-only.
- **Spec:** P2-PAGE-004
- **Touches (new):** `src-tauri/src/pdf/insert_blank.rs`
- **What:** Insert at index. Inherit MediaBox + orientation from the adjacent page unless caller passes overrides.
- **Acceptance:** Insert blank after page 2 of a Letter-orientation doc → new page is Letter, blank, between original page 2 and 3.
- **Test:** `src-tauri/tests/insert_blank.rs`.
- **Depends on:** P2.A1, P2.A3.

### P2.B4. `[x]` Crop page (CropBox only)
- **Human-verified (2026-06-13):** cropped artifact + in-app crop/reset/undo confirmed (CropBox-only, per spec). Two follow-ups surfaced and filed to BACKLOG (do **not** block B4): (1) crop not reflected in the thumbnail re-render; (2) crop (CropBox) not carried into split output via `FPDF_ImportPages`.
- **Status:** Landed. `CropEdit: Edit<PdfDocument>` (`pdf/crop.rs`) sets
  `/CropBox` via `boundaries_mut().set_crop` (content untouched); `None`
  resets to the `/MediaBox`. Inverse captures + restores the previous box
  (reset is itself undoable). Actor `CropPage`; `pdf_crop_page`; a margin-
  input **CropDialog** (`src/tools/crop/`) opened from the thumbnail menu
  ("Crop page…") + "Reset crop". Full-reload preview (CropBox changes the
  viewport size) + undo + autosave inherited. `check` + `test` (100/100) +
  `test:rust` (crop: 5) green. `[~]` until the human checks
  `/tmp/vibepdf-verify-cropped.pdf` (Desktop) in Acrobat + Preview + a third
  reader, and the GUI flow.
- **Gotcha handled:** pdfium-render's `crop()` *errors* when a page has no
  explicit `/CropBox`; we fall back to `media()` for the effective box.
- **UI scope:** margins dialog (points). Drag-select overlay → BACKLOG
  (backend already takes any rect).
- **Spec:** P2-PAGE-009
- **Touches (new):** `src-tauri/src/pdf/crop.rs`, `src/tools/crop/`
- **What:** Adjust `/CropBox` on the page dict without altering content streams. Reversible by setting CropBox back to MediaBox.
- **Acceptance:** Crop page 1 → save → re-open → only the cropped region is visible. Run "reset crop" → MediaBox restored.
- **Test:** `src-tauri/tests/crop.rs` — assert CropBox dict matches expected rectangle.
- **Depends on:** P2.A1, P2.A3.

### P2.B5. `[~]` Resize page (standard + custom)
- **Status:** Landed. `ResizeEdit` scales content to a new size via the **lopdf
  byte-handoff** (`cos::resize_pages`), not PDFium: each target page's content
  stream is wrapped with `q <scale-matrix> cm … Q` and its `/MediaBox` set to the
  new size; the doc is reloaded, replacing the actor's document. Undo via
  `RestoreDocEdit` (pre-resize snapshot). Actor `ResizePages`; `pdf_resize_pages`;
  a preset/custom **ResizeDialog** (`tools/resize/`, presets in `page-sizes.ts`)
  from the thumbnail menu ("Resize page…") with a This-page/All-pages scope.
  Preserve-aspect scales uniformly + centres; otherwise stretches. `check` +
  `test` (145/145, incl. `page-sizes` 4) + `test:rust` (resize: 5 + cos resize: 1)
  green. `[~]` until the human checks `Sample PDFs/vibepdf-verify-resized.pdf`
  (Letter→A4, content scaled) in Preview + Chrome, and the in-app flow.
- **Why lopdf, not PDFium (key deviation from the plan):** PDFium's page-content
  transform (`FPDFPage_TransFormWithClip`, and the macro `page.scale()`/`transform()`)
  forces a `reload_in_place` (pdfium-render #93) that **SIGSEGVs at teardown** —
  reproduced in tests. The COS content-stream wrap avoids PDFium's content API
  entirely. Recorded in `docs/04` "Structural edits via lopdf".
- **Limits (BACKLOG):** annotations aren't re-scaled (`/Rect`s keep their coords);
  `/CropBox` (+ Bleed/Trim/Art) is dropped so it defaults to the new MediaBox;
  orientation isn't auto-matched; dialog takes custom size in points (no mm/in yet).
- **Spec:** P2-PAGE-010
- **Touches (new):** `src-tauri/src/pdf/resize.rs`, `src/ipc/resize.ts`,
  `src/tools/resize/{page-sizes.ts,page-sizes.test.ts,ResizeDialog.tsx}`;
  (modify) `cos.rs` (`resize_pages`), `actor.rs`, `commands/pdf.rs`, `lib.rs`,
  `ThumbnailPanel.tsx`, `RotateMenu.tsx`.
- **What:** Resize to Letter / A4 / Legal / A3 / A5 / Tabloid / custom W×H. Optional "preserve aspect ratio" scales content; without it, content is scaled to fit and may stretch.
- **Acceptance:** Resize an A4 doc to Letter → save → re-open → MediaBox 612×792, content scaled. *(Demo runs Letter→A4 — all fixtures are Letter.)*
- **Test:** `src-tauri/tests/resize.rs` (+ `cos_resizes_sets_mediabox_and_wraps_content`).
- **Depends on:** P2.A1, P2.A3, lopdf (cos.rs).

---

## Track C — Multi-page operations

### P2.C1. `[x]` Reorder via thumbnail drag
- **GUI drag was broken, now fixed + human-verified (2026-06-13).** The backend
  always worked; the **HTML5 drag-and-drop never completed in the macOS Tauri
  webview**. Instrumented logging proved WKWebView fires `dragstart`/`dragend`
  but **never delivers `dragenter`/`dragover`/`drop`** — so the drop handler
  could not run, on any PDF (the nested-tree theory was wrong; it failed on a
  flat fixture too). **Fix:** reimplemented the reorder with **pointer events**
  (`pointerdown`/`move`/`up`) on the `<ul>` — a 6px threshold separates click
  (select) from drag, the source tile dims + the hovered tile shows a ring, and
  the drop tile is found via `data-thumb-tile` + `elementFromPoint`. No HTML5
  DnD anywhere now. Verified live: drag reorders pages in the sidebar + main
  view, ⌘Z/⌘⇧Z work, plain click still selects. `tsc` + `eslint src` + `test`
  (141/141, reorder helper 9/9) green; no Rust changed. See `docs/04`
  "WKWebView quirks".
- **Status:** Landed. First feature on the lopdf byte-handoff.
  `pdf/reorder.rs::ReorderEdit` serializes the live doc → `cos::reorder_pages`
  (permute the root `/Pages` `/Kids`) → reloads the result via
  `load_pdf_from_byte_vec`, **replacing** the actor's document (old doc dropped
  under the lock). Undoable: the inverse is the inverse permutation (just a
  `Vec<usize>` — no byte snapshots). Object-ref links/bookmarks/dests track the
  move for free. Actor `ReorderPages`; `pdf_reorder_pages`; **thumbnail
  drag-and-drop** in `ThumbnailPanel` (`tools/reorder/compute-reorder.ts`
  `movePage`) → reorder → bump epoch + sync undo/redo. `check` + `test`
  (141/141) + `test:rust` (reorder: 2, cos reorder: 2) green. `[~]` until the
  human checks `Sample PDFs/vibepdf-verify-reordered.pdf` in Acrobat + Preview.
- **Notes:** **flat page tree only** — `cos::reorder_pages` validates the root
  `/Kids` are exactly the page leaves and errors clearly on a nested tree
  (never corrupts; round-trip-verify is the backstop). Nested-tree reorder
  (flatten + inheritance push-down) is a BACKLOG follow-up. Object-ref
  reference integrity verified (the link page moves correctly); the
  cross-reader ritual confirms the link still lands.
- **Spec:** P2-PAGE-002
- **Touches (new):** `src-tauri/src/pdf/reorder.rs`, `src/ipc/reorder.ts`,
  `src/tools/reorder/compute-reorder.ts`; (modify) `cos.rs`, `ThumbnailPanel.tsx`.
- **What:** Drag-drop within the thumbnail panel calls `pdf_reorder_pages`. Backend renumbers pages and updates all internal references (links, bookmarks, named destinations).
- **Acceptance:** Drag page 1 to the end of a 5-page doc → save → re-open → original page 1 is now page 5; bookmark that pointed to old page 1 now points to new page 5.
- **Test:** `src-tauri/tests/reorder.rs`.
- **Depends on:** P2.A1, P2.A3, P1.D1 (thumbnails panel must exist), lopdf (cos.rs).

### P2.C2. `[x]` Extract pages to new PDF
- **Human-verified (2026-06-13):** extracted artifact + in-app extract confirmed; source doc unchanged.
- **Status:** Landed. `pdf/extract.rs::extract_pages` builds a new doc via
  `create_new_pdf` + `copy_pages_from_document` (FPDF_ImportPages, copies
  resources) and writes it through A1's verified `save_document` (atomic +
  round-trip). **Read-only on the source** — not an edit, no undo/dirty.
  Actor `ExtractPages`; `pdf_extract_pages`; a range-input **ExtractDialog**
  (`tools/extract/page-range.ts` parser) + a native save dialog, triggered
  from the viewer toolbar ("Extract…"). Reuses `delete_page`'s
  `validate`/`range_string` (now `pub(crate)`). `check` + `test` (108/108) +
  `test:rust` (extract: 3) green. `[~]` until the human checks
  `/tmp/vibepdf-verify-extracted.pdf` (Desktop) in Acrobat + Preview + a
  third reader.
- **Notes:** dialog page-count comes from the live PDF.js `numPages`; the
  backend re-validates. `links.pdf` uses standard Helvetica (not embedded),
  so the "no missing glyph" check rests on the cross-reader ritual — an
  embedded-font fixture would strengthen it (BACKLOG). **Unblocks C3 (split).**
- **Spec:** P2-PAGE-006
- **Touches (new):** `src-tauri/src/pdf/extract.rs`, `src/ipc/extract.ts`
- **What:** Produce a new PDF containing the selected pages, copying or referencing all required resources (fonts, images, color spaces).
- **Acceptance:** Extract pages 2-3 of a 10-page PDF with embedded fonts → resulting file opens cleanly in Acrobat with no missing-glyph rendering.
- **Test:** `src-tauri/tests/extract.rs` — extract pages, open output via PDFium, assert page count + resource integrity (no dangling indirect refs).
- **Depends on:** P2.A1.

### P2.C3. `[~]` Split (4 modes)
- **Status:** Landed. `pdf/split.rs::split_document` reduces every mode to a
  list of contiguous 0-based page groups, then writes one file per group via
  the shared `extract::write_subset_pdf` (refactored out of C2 — atomic +
  round-trip-verified per file). Output files: `{stem}-NNN.pdf` in a chosen
  directory. **Read-only on the source** — no undo/dirty. Actor
  `SplitDocument`; `pdf_split_document`; a mode-picker **SplitDialog**
  (`tools/split/split-points.ts` parser for mode b) + a native directory
  picker, triggered from the viewer toolbar ("Split…"). `check` + `test`
  (117/117) + `test:rust` (split: 6) green. `[~]` until the human checks
  `~/Desktop/vibepdf-verify-split-00{1,2,3}.pdf` in Acrobat + Preview + a
  third reader.
- **Notes:** modes — (a) every N, (b) split-before 1-based pages, (c) greedy
  by-size (serialize-probe; approximate, never drops a page), (d) top-level
  bookmarks (read-only outline via PDFium — *no* lopdf needed). A split that
  would yield < 2 files is reported as an error rather than emitting one
  copy. By-size re-serializes per page (O(n²)) — fine for one-shot; noted in
  BACKLOG. New fixture `bookmarks.pdf` (6 pp, 3 top-level bookmarks → pages
  0/2/4). Cross-split reference integrity (dangling links/dests) is the same
  lopdf wall as B2/C1 — out of scope.
- **Spec:** P2-PAGE-007
- **Touches (new):** `src-tauri/src/pdf/split.rs`, `src/app/SplitDialog.tsx`,
  `src/ipc/split.ts`, `src/tools/split/split-points.ts`,
  `tests/fixtures/basic/bookmarks.pdf` (+ generator)
- **What:** Four modes — (a) every N pages, (b) at specific page numbers, (c) by file-size target, (d) by top-level bookmarks. Each emits N output files.
- **Acceptance:** Split a 100-page PDF every 10 pages → 10 files, each opens cleanly. Split by file-size target of 5MB on a 50MB PDF → expected output count.
- **Test:** `src-tauri/tests/split.rs` — each of the four modes.
- **Depends on:** P2.C2.

### P2.C4. `[x]` Merge multiple PDFs (full P2-PAGE-008)
- **Human-verified (2026-06-13):** `vibepdf-verify-merged.pdf` — 7 pages + form field on the last page (Preview), and the **3-bookmark outline (Chapter 1/2/3) confirmed in Chrome's PDF outline sidebar** (Preview hides outlines by default — a Preview limitation, not a defect; bytes also carry `/Outlines`, `/Count 3`, valid `/Dest`). Full P2-PAGE-008 (bookmarks + form fields preserved) met.
- **Status:** Landed (completed). Now an **all-lopdf merge**
  (`cos::merge_documents`): each source's object graph is renumbered to a
  disjoint id range and combined into one document, then `/Pages`, `/Catalog`,
  `/Outlines`, and `/AcroForm` are rebuilt — so pages, content, annotations,
  **bookmarks** (one outline subtree per source), and **form fields** all
  survive, with colliding `/T` names suffixed (`name` → `name_2`). `merge.rs`
  reads source bytes → `cos::merge_documents` → loads the result into PDFium →
  A1's verified `save_document`. UI unchanged (MergeDialog / toolbar "Merge…").
  `check` + `test` (141/141) + `test:rust` (merge: 7 incl. bookmark + form-
  field-rename; cos: +1) green. `[~]` until the human checks
  `Sample PDFs/vibepdf-verify-merged.pdf` (bookmarks + a form field) in Acrobat
  + Preview.
- **Engine swap:** replaced the earlier `FPDF_ImportPages` merge (which dropped
  `/Outlines` + `/AcroForm`). All pre-existing merge tests (page count,
  annotation survival, order, guards) kept as **regression guards** — green.
  The old `merge_does_not_yet_carry_bookmarks` tripwire is now
  `merge_carries_bookmarks`. **Limits:** top-level `/T` rename only (kid-field
  hierarchies → follow-up); page-attribute-inheritance flatten edge shared with
  C1's nested-tree BACKLOG item.
- **Spec:** P2-PAGE-008
- **Touches:** `src-tauri/src/pdf/cos.rs` (merge), `src-tauri/src/pdf/merge.rs`
  (engine swap); `MergeDialog`/`ipc/merge.ts`/`tools/merge/reorder.ts` unchanged.
- **What:** Concatenate N PDFs preserving annotations, form fields, bookmarks. On form-field name collision, suffix `_2`, `_3`, etc. Resulting bookmarks remain navigable.
- **Acceptance:** Merge 5 PDFs each with bookmarks and form fields → resulting file has all bookmarks (one tree per source) and all form fields with collision-free names. *(Bookmark/form-field criteria pending lopdf.)*
- **Test:** `src-tauri/tests/merge.rs`.
- **Depends on:** P2.A1.

---

## Track D — From-other-PDF

### P2.D1. `[x]` Insert pages from another PDF (full P2-PAGE-005)
- **Human-verified (2026-06-13):** `vibepdf-verify-insertfrom.pdf` — 5 pages with the inserted source pages (first keeps its annotation), and the **form field on the last page renders and is fillable, confirmed in Chrome** (it was camouflaged white-on-white in Preview — cosmetic, filed to BACKLOG). Full P2-PAGE-005 (form fields preserved) met. Two cosmetic/UX follow-ups remain in BACKLOG (faint widget border; reversed-range `99-82` normalize) — neither blocks the step.
- **Status:** Landed (completed). `pdf/insert_from.rs::InsertFromEdit` copies
  the selected source pages into the open doc at `index` via PDFium
  (`copy_pages_from_document` — content, annotations, dimensions), then a lopdf
  pass (`cos::register_inserted_form_fields`) re-attaches the inserted pages'
  terminal **form fields** into `/AcroForm`, suffixing colliding `/T` names
  (`name` → `name_2`). Because that `/AcroForm` change isn't piecemeal-
  undoable, the inverse is now a **`RestoreDocEdit`** (P2 new) holding the
  pre-insert document bytes: undo restores that snapshot, redo the post-insert
  one (pages + form round-trip). Actor `InsertFromPdf`; `pdf_insert_from_pdf` +
  read-only `pdf_peek_page_count`; **InsertFromDialog** from the toolbar
  ("Insert PDF…") unchanged. `check` + `test` (141/141) + `test:rust`
  (insert_from: 8 incl. form-field preserve + collision rename; cos: +1) green.
  `[~]` until the human checks `Sample PDFs/vibepdf-verify-insertfrom.pdf`
  (an inserted form field) in Acrobat + Preview.
- **Limits:** terminal form fields only (widget carrying its own `/T`) — kid/
  hierarchical fields are a follow-up (shared with C4). `RestoreDocEdit` holds
  a full-doc byte snapshot per insert in the undo stack (BACKLOG note).
- **Spec:** P2-PAGE-005
- **Touches (new):** `src-tauri/src/pdf/insert_from.rs`, `src-tauri/src/pdf/restore.rs`,
  `src/app/InsertFromDialog.tsx`, `src/ipc/insert-from.ts`, `src/ipc/peek.ts`;
  (modify) `cos.rs`.
- **What:** Open a second PDF, copy specified pages into the current doc at a target index, preserving content, annotations, form fields, page dimensions.
- **Acceptance:** Insert page 1 of `B.pdf` (with annotations) into `A.pdf` at index 3 → resulting `A.pdf` has the annotation on the new page 4. *(Form-field preservation pending lopdf.)*
- **Test:** `src-tauri/tests/insert_from.rs`.
- **Depends on:** P2.A1, P2.A3.

---

## Suggested order

P2.A1 → P2.A3 → P2.A2 → P2.B1 → P2.B2 → P2.B3 → P2.C1 → P2.C2 → P2.C4 → P2.B4 → P2.B5 → P2.C3 → P2.D1

Track A *must* land first — every subsequent step writes through `Save`
and pushes onto the undo stack. After that, B steps are quick wins and
C steps deliver the headline roadmap acceptance demo.
